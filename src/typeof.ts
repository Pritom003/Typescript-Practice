{
    // A string literal type is a type that represents an exact, specific string. When a variable is assigned a string literal type, it can only hold that specific string value.
const Tname ='something'
type personName=typeof Tname 
const name :personName='something'// it has to be the same string  

















}